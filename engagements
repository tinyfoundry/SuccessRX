<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Consulting & Engagements | SuccessRX</title>
    <style>
        /* --- EXACT CSS COPIED FROM INDEX.HTML --- */
        :root {
            --primary: #0284c7; /* Clinical Blue */
            --primary-dark: #0369a1;
            --accent: #e11d48; /* Vital Red */
            --dark: #0f172a;
            --light: #f8fafc;
            --gray: #64748b;
            --white: #ffffff;
            --border: #e2e8f0;
            --font-main: 'Segoe UI', Roboto, Helvetica, Arial, sans-serif;
            --shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06);
        }

        * { margin: 0; padding: 0; box-sizing: border-box; scroll-behavior: smooth; }
        
        body {
            font-family: var(--font-main);
            color: var(--dark);
            background-color: var(--light);
            line-height: 1.6;
            overflow-x: hidden;
            display: flex;
            flex-direction: column;
            min-height: 100vh;
        }

        a { text-decoration: none; color: inherit; transition: 0.3s; }
        ul { list-style: none; }

        /* --- UTILITIES --- */
        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 20px;
        }
        .section-title {
            text-align: center;
            margin-bottom: 50px;
        }
        .section-title h2 {
            font-size: 2.5rem;
            color: var(--dark);
            margin-bottom: 10px;
        }
        .section-title p {
            color: var(--gray);
            font-size: 1.1rem;
        }
        
        /* --- HEADER (CENTERED LOGIC) --- */
        header {
            background: var(--white);
            border-bottom: 1px solid var(--border);
            position: sticky;
            top: 0;
            z-index: 1000;
            padding: 15px 0;
        }
        nav {
            position: relative;
            display: flex;
            justify-content: center;
            align-items: center;
        }
        .logo {
            font-size: 1.5rem;
            font-weight: 800;
            color: var(--primary);
            display: flex;
            align-items: center;
            gap: 8px;
            position: absolute;
            left: 0;
            top: 50%;
            transform: translateY(-50%);
        }
        .logo span { color: var(--dark); }
        .nav-links {
            display: flex;
            gap: 30px;
        }
        .nav-links a {
            font-weight: 500;
            color: var(--gray);
        }
        .nav-links a:hover { color: var(--primary); }
        .nav-links a.active { color: var(--primary); font-weight: 700; }
        .mobile-toggle { 
            display: none; 
            font-size: 1.5rem; 
            cursor: pointer;
            position: absolute;
            right: 0;
            top: 50%;
            transform: translateY(-50%);
        }

        /* --- HERO --- */
        .hero {
            background: linear-gradient(135deg, #f0f9ff 0%, #e0f2fe 100%);
            padding: 80px 0;
            text-align: center;
        }
        .hero h1 {
            font-size: 3rem;
            color: var(--dark);
            margin-bottom: 15px;
        }
        .hero p {
            font-size: 1.2rem;
            color: var(--gray);
            max-width: 700px;
            margin: 0 auto;
        }

        /* --- SERVICES GRID --- */
        .services-section { padding: 60px 0; flex: 1; }
        .grid-3 {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
            gap: 30px;
        }

        .service-card {
            background: var(--white);
            border: 1px solid var(--border);
            border-radius: 12px;
            padding: 30px;
            box-shadow: var(--shadow);
            display: flex;
            flex-direction: column;
            transition: transform 0.2s;
            position: relative;
            overflow: hidden;
        }
        .service-card:hover { transform: translateY(-5px); border-color: var(--primary); }
        
        .service-header { margin-bottom: 20px; border-bottom: 1px solid var(--light); padding-bottom: 15px; }
        .service-header h3 { font-size: 1.4rem; color: var(--dark); margin-bottom: 5px; }
        .service-header .badge { 
            display: inline-block; 
            background: #e0f2fe; 
            color: var(--primary); 
            font-size: 0.75rem; 
            font-weight: 700; 
            padding: 4px 8px; 
            border-radius: 4px; 
            text-transform: uppercase;
        }
        
        .service-body ul { margin: 20px 0; padding-left: 20px; list-style: disc; color: var(--gray); }
        .service-body li { margin-bottom: 10px; }
        .service-body p { color: var(--gray); font-size: 0.95rem; }

        .price-tag {
            margin-top: auto;
            font-weight: 700;
            color: var(--dark);
            font-size: 1.1rem;
            margin-bottom: 15px;
        }

        /* --- FOOTER --- */
        footer {
            background: var(--dark);
            color: #94a3b8;
            padding: 30px 0;
            text-align: center;
            font-size: 0.9rem;
            margin-top: auto;
        }

        /* --- RESPONSIVE --- */
        @media (max-width: 768px) {
            .nav-links { display: none; }
            .mobile-toggle { display: block; }
            .hero h1 { font-size: 2.2rem; }
        }
    </style>
</head>
<body>

    <!-- HEADER -->
    <header>
        <div class="container">
            <nav>
                <div class="logo">Success<span>RX</span></div>
                <ul class="nav-links">
                    <li><a href="curriculum.html">Curriculum</a></li>
                    <li><a href="events.html" class="active">Engagements</a></li>
                    <li><a href="reviews.html">Reviews</a></li>
                    <li><a href="contact.html">Contact</a></li>
                </ul>
                <div class="mobile-toggle" onclick="toggleMenu()">☰</div>
            </nav>
        </div>
    </header>

    <!-- HERO -->
    <section class="hero">
        <div class="container">
            <h1>Consulting & Engagements</h1>
            <p>Prescriptive solutions to elevate your Customer Success maturity. Choose the engagement that fits your organization's needs.</p>
        </div>
    </section>

    <!-- SERVICES GRID -->
    <section class="services-section">
        <div class="container">
            <div class="grid-3">

                <!-- Service 1: Chairsides -->
                <div class="service-card">
                    <div class="service-header">
                        <span class="badge">Assessment</span>
                        <h3>Chairsides</h3>
                    </div>
                    <div class="service-body">
                        <p>A deep-dive maturity assessment for your CS organization. We sit down with your practitioners to diagnose workflow health and prescribe necessary corrections.</p>
                        <ul>
                            <li>1:1 Interviews with Senior CSMs</li>
                            <li>Current State Playbook Audit</li>
                            <li>Maturity Model Scoring</li>
                            <li>Prescription for Best Practices</li>
                        </ul>
                    </div>
                    <div class="price-tag">Est. 10-Hour Engagement</div>
                    <a href="contact.html" class="btn" style="background:var(--primary); color:white; padding:12px; border-radius:6px; text-align:center;">Request Assessment</a>
                </div>

                <!-- Service 2: 1:1 Coaching -->
                <div class="service-card">
                    <div class="service-header">
                        <span class="badge">Mentorship</span>
                        <h3>1:1 Executive Coaching</h3>
                    </div>
                    <div class="service-body">
                        <p>Personalized coaching sessions for CS Leaders or aspiring VPs. We focus on leadership skills, strategy execution, and career development.</p>
                        <ul>
                            <li>Flexible Scheduling (Weekly/Bi-weekly)</li>
                            <li>Goal & OKR Setting</li>
                            <li>Real-time Scenario Roleplay</li>
                            <li>Unlimited Async Feedback (Slack/Email)</li>
                        </ul>
                    </div>
                    <div class="price-tag">Retainer or Hourly Rate</div>
                    <a href="contact.html" class="btn" style="background:var(--primary); color:white; padding:12px; border-radius:6px; text-align:center;">Book Coaching</a>
                </div>

                <!-- Service 3: CS Platform Strategy -->
                <div class="service-card">
                    <div class="service-header">
                        <span class="badge">Technical Strategy</span>
                        <h3>CS Platform Strategy</h3>
                    </div>
                    <div class="service-body">
                        <p>"Art of the Possible" discovery and implementation strategy for your CS Platform (e.g., Gainsight, ChurnZero, Catalyst). We transform your tech stack into a data engine.</p>
                        <ul>
                            <li>Platform Architecture Audit</li>
                            <li>Data Schema & Object Strategy</li>
                            <li>Health Score Configuration</li>
                            <li>Automated Journey Mapping</li>
                        </ul>
                    </div>
                    <div class="price-tag">Project-Based</div>
                    <a href="contact.html" class="btn" style="background:var(--primary); color:white; padding:12px; border-radius:6px; text-align:center;">Consult Now</a>
                </div>

                <!-- Service 4: AI Enablement -->
                <div class="service-card">
                    <div class="service-header">
                        <span class="badge">Innovation</span>
                        <h3>AI Enablement</h3>
                    </div>
                    <div class="service-body">
                        <p>Modernize your CS workflow with Artificial Intelligence. We audit your current tools and adoption to build a robust enablement plan.</p>
                        <ul>
                            <li>Current AI Tool Inventory & Audit</li>
                            <li>Adoption & Usage Analysis</li>
                            <li>SWOT Analysis of AI Strategy</li>
                            <li>Future State Implementation Roadmap</li>
                        </ul>
                    </div>
                    <div class="price-tag">Project-Based</div>
                    <a href="contact.html" class="btn" style="background:var(--primary); color:white; padding:12px; border-radius:6px; text-align:center;">Enable AI</a>
                </div>

            </div>
        </div>
    </section>

    <!-- FOOTER -->
    <footer>
        <div class="container">
            <p>&copy; 2023 SuccessRX. All Rights Reserved.</p>
        </div>
    </footer>

    <script>
        // Toggle Mobile Menu
        function toggleMenu() {
            const navLinks = document.querySelector('.nav-links');
            if (navLinks.style.display === 'flex') {
                navLinks.style.display = 'none';
            } else {
                navLinks.style.display = 'flex';
                navLinks.style.flexDirection = 'column';
                navLinks.style.position = 'absolute';
                navLinks.style.top = '60px';
                navLinks.style.right = '0';
                navLinks.style.background = 'white';
                navLinks.style.width = '100%';
                navLinks.style.padding = '20px';
                navLinks.style.borderBottom = '1px solid #ddd';
            }
        }
    </script>
</body>
</html>
